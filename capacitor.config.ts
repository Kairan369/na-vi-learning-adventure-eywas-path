import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: "Na'vi Learning Adventure: Eywa's Path",
  webDir: 'dist'
};

export default config;
